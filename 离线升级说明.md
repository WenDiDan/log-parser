# 离线升级说明（无网 / 内网隔离现场专用）

**适用场景**：现场设备**无外网**（连不上 Gitee / GitHub），或出于安全策略禁止外联，
但仍希望用 LogParser 自带的「检查更新 → 下载 → 替换」机制完成版本升级。

---

## 一、原理（一句话）

把升级用的 `version.json` + `LogParser.exe` 放在**同一个本地目录 / U 盘 / 内网共享**，
客户端把升级源第 1 行指向这个 `version.json`，检查更新时优先读本地文件、下载同目录的 exe。

- 第 1 源 = 本地（离线，优先）
- 第 2 源 = Gitee（联网才会用）
- 第 3 源 = GitHub（联网才会用）

有网时自动走 Gitee/GitHub；无网时自动落到本地源——**同一份客户端，两种环境都通**。

> 代码里本地相对 url 解析走 `os.path.join(dirname(清单), url)` 分支，
> 不受 Windows 反斜杠影响，exe 一定落在与清单**同目录**。无需担心路径拼接出错。

---

## 二、制作离线升级包（一次性 / 每次发版）

准备一个目录（示例用 U 盘 `D:\LogParser_update\`），放两个文件：

```
D:\LogParser_update\
├── version.json      ← 升级清单（相对 url）
└── LogParser.exe     ← 新版本安装包（与清单同目录）
```

### `version.json` 模板（**url 必须用相对路径 `LogParser.exe`**）

```json
{
  "version": "1.0.8",
  "url": "LogParser.exe",
  "notes": "离线升级包：将本目录作为升级源第1行指向 version.json 即可。"
}
```

> ⚠️ 离线包里 `url` 写 **`"LogParser.exe"`（相对）**，不要写绝对直链。
> 程序会用清单所在目录自动拼出同目录的 exe，U 盘换盘符也能用。
>
> （对比：Gitee/GitHub 那边的清单要用**绝对直链**，因为那是 Web 地址；
> 本地清单用相对路径，因为程序按文件目录拼接。两者规则不同，别混。）

### `LogParser.exe` 来源

用开发机打包好的最新版 `dist/LogParser.exe` 复制过来即可（版本号须与 `version.json` 的 `version` 一致）。

---

## 三、现场客户端配置（每台设备一次）

1. 打开 LogParser → 菜单 **帮助 → 升级源设置…**
2. 把**第 1 行**改成离线包里 `version.json` 的实际路径，例如：
   ```
   D:\LogParser_update\version.json
   ```
   （U 盘盘符、内网 UNC 共享 `\\server\share\LogParser\version.json` 均可）
3. 点击「保存」。此时升级源顺序为：本地 → Gitee → GitHub。
4. **菜单 → 检查更新**：
   - 无网时直接读本地清单，版本更高则提示升级，下载同目录 exe 完成替换；
   - 若本地清单版本等于当前版本，显示「已是最新」。

> 想确认当前生效的源：菜单 **帮助 → 升级源设置…** 里看到的就是实际顺序。
> 也可删除第 2/3 行只留本地源，做成「纯离线」模式（联网也不会去查 Gitee/GitHub）。

---

## 四、批量部署（不逐台点）

把写好的 `config.json` 随离线包一起发到现场机即可，开机即生效。

`config.json` 位置（每台 Windows 用户目录）：
```
C:\Users\<用户名>\.logparser\config.json
```

关键字段：
```json
{
  "update_manifests": [
    "D:\\LogParser_update\\version.json",
    "https://gitee.com/WenDiDan/log-parser/raw/main/LogParser/version.json",
    "https://github.com/WenDiDan/log-parser/releases/latest/download/version.json"
  ]
}
```

- 第 1 个源指向现场约定好的更新目录（U 盘 / 内网共享）。
- 之后每次发版：只更新该目录下的 `version.json` + `LogParser.exe`，客户端检查更新即生效，
  **无需再改每台设备的配置**。

---

## 五、验证（发版后在现场抽测）

1. 现场机打开客户端，菜单 → **检查更新**。
2. 期望：本地清单版本 > 当前版本 → 弹出升级窗口 → 进度走完 → 自动重启为新版本。
3. 升级后菜单 → **关于** 确认版本号已变。

若显示「已是最新」：
- 检查 `version.json` 的 `version` 是否**大于**当前运行版本；
- 检查 exe 与清单是否**同目录**；
- 检查升级源第 1 行路径是否真的指向这个 `version.json`。

---

## 六、注意事项

- **清单与 exe 必须同目录**，且 `version.json` 的 `url` 为 `"LogParser.exe"`（相对）。
- 离线包版本号要**等于或高于**现场已装版本，否则不会提示升级。
- U 盘换盘符不影响：相对 url 由程序按实际目录实时拼接。
- 内网 UNC 共享（如 `\\192.168.x.x\share\...`）同样支持，前提是该共享对现场机可读。
- 联网环境下本地源仍然优先；若想强制走 Gitee/GitHub，把本地源从列表移除即可。
- 离线包不含 Gitee/GitHub 清单里的「绝对直链」要求，那种规则只用于 Web 发布源（见《发布指南》）。
